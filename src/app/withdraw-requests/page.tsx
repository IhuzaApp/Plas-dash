import React from 'react';
import WithdrawRequests from '@/components/pages/WithdrawRequests';

export const metadata = {
    title: 'Withdraw Requests | Plas Dashboard',
    description: 'Manage and process withdraw requests',
};

export default function WithdrawRequestsPage() {
    return <WithdrawRequests />;
}
